#include <stdio.h>

int main2()
{
    int media, faltas;
    printf("%s", "Digite a média");
    scanf("%d", &media);
    printf("%s", "Digite o número de faltas");
    scanf("%d", &faltas);
//Fluxograma Simples
    if (media >= 5)
       printf("%s", "Aluno Aprovado");
}
